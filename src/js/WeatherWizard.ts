// main class
// trying to change remote
export class WeatherWizard {

    /**
     * Get's weather data from some api
     */
    function getWeatherData() {

    }

    /**
     * input parameter is the city the user entered / GPS found
     */
    function getCityData(city: string) {
        // const locationData = api.openweathermap.org/data/2.5/forecast?id={city ID};

    }


    // each time we make an API call, we need to call http://api.openweathermap.org/data/2.5/forecast?id=524901&APPID={178e8d4180edebe4e2c02fcad75b72fd
}